import { SelectedElectivesPipe } from './selected-electives.pipe';

describe('SelectedElectivesPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectedElectivesPipe();
    expect(pipe).toBeTruthy();
  });
});
